(function () {
	"use strict";

	module.exports = function (app, twitterHelper, nluHelper) {
		app.get("/analyze", function (req, res) {
			
			/**
			 * Get Tweets
			 * Process Tweets
			 * Analyze with NLU
			 * Show the results
			 **/
		});
	}
}())